# StratifyX Database

The schema in this folder is the initial PostgreSQL foundation for StratifyX.

- `schema.sql`: DDL for core entities
- `seed.sql`: starter demo data

The running application does not require a live PostgreSQL instance yet. Phase 2 will move the API from demo data to real persistence using this schema as the starting point.
